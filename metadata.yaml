name: prometheus-relay
summary: cross-model relay for prometheus interface
description: cross-model relay for prometheus interface
requires:
  frontend:
    interface: prometheus
    relay: some-monitoring
provides:
  backend:
    interface: prometheus
    relay: some-monitoring
series:
  - xenial
